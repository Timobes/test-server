import { i18n, JSX } from 'typedoc';
export function AnchorIconLink({ context, anchor }) {
    if (!anchor)
        return null;
    return (JSX.createElement("a", { href: `#${anchor}`, "aria-label": i18n.theme_permalink(), class: 'anchor-icon' }, context.icons.anchor()));
}
