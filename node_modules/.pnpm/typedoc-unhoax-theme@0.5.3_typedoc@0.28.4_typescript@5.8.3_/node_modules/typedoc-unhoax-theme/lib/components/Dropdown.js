import { JSX } from 'typedoc';
export function Dropdown(props) {
    return (JSX.createElement("span", { class: 'dropdown' },
        props.trigger,
        JSX.createElement("div", { class: ['dropdown-overlay', props.overlayClass].filter(Boolean).join(' ') }, props.children)));
}
