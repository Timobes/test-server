import { JSX } from 'typedoc';
export function SidebarLinks({ context }) {
    const links = Object.entries(context.options.getValue('sidebarLinks'));
    if (links.length === 0)
        return null;
    return (JSX.createElement("nav", { class: 'sidebar-links' }, links.map(([label, url]) => (JSX.createElement("a", { href: url }, label)))));
}
