import { JSX } from 'typedoc';
export function MemberSignatures({ context, props }) {
    return (JSX.createElement("div", { class: `signatures ${context.getReflectionClasses(props)}` }, props.signatures?.map((item) => (JSX.createElement("div", { class: 'signature' },
        JSX.createElement("pre", { class: 'anchor-link' },
            JSX.createElement("code", null, context.memberSignatureTitle(item))),
        JSX.createElement("div", { class: 'signature-body' }, context.memberSignatureBody(item)))))));
}
