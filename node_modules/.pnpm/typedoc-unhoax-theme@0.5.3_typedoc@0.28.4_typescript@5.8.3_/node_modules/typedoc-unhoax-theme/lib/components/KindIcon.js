import { JSX, ReflectionKind } from 'typedoc';
export function KindIcon({ kind }) {
    const kindName = getEnumKeyOf(ReflectionKind, kind);
    if (!kindName)
        return null;
    return (JSX.createElement("abbr", { title: kindName, class: 'kind-icon' },
        "(",
        kindName[0],
        ")"));
}
function getEnumKeyOf(theEnum, value) {
    const key = Object.keys(theEnum).find((key) => theEnum[key] === value);
    if (!key)
        throw new Error(`Value ${value} not found in enum ${theEnum}`);
    return key;
}
